# Step 02 — Run

1. Follow the BMAD runner instructions in your environment to execute `workflow-init`.
2. Typical command: use the BMAD tooling or scripts that load `_bmad` and execute the workflow XML/MD.
